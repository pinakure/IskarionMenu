networks:
  proxy_primary-network:
    external: true
services:
  menu-service:
    image: nginx:alpine
    container_name: hq-menu
    hostname: hq-menu
    networks:
      - proxy_primary-network
    volumes:
      - /src/menu/web:/usr/share/nginx/html:ro
    restart: unless-stopped
